// Nurse referrals removed — nurses don't manage referrals in this UI.
// Component intentionally left as a no-op to avoid build errors if still referenced elsewhere.
import React from "react";

export default function NurseReferral() {
  return null;
}
